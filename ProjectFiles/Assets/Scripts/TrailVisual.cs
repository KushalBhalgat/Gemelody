using System.Collections;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.VFX;
using UnityEngine.Experimental.VFX;

public class TrailVisual : MonoBehaviour {

    /*
    [SerializeField] private VisualEffect trail;
    [SerializeField] private Color col;
    [SerializeField] private GameObject targetGameObject;

    private void Start() {
        trail.SetVector3("ParticlePosition", (-targetGameObject.GetComponent<Enemy>().targetDir).normalized);
        trail.SetFloat("Thrust", Random.Range(20, 30f));
    }


    private void Update() {
    }
    */
}
